import 'package:flutter/material.dart';

class VisitOurWebsitePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('VisitOurWebsitePage'),
        backgroundColor: Colors.green,
      ),
    );
  }
}
