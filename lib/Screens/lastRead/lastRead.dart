import 'package:flutter/material.dart';

class LastReadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Last read Page'),
        backgroundColor: Colors.green,
      ),
    );
  }
}
